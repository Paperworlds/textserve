[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "gordon-mcp"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "mcp>=1.0.0",
]

[tool.hatch.build.targets.wheel]
packages = ["src/gordon_mcp"]
