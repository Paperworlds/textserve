protocol: stdio
runtime: claude
native_cmd: uv
native_args:
  - run
  - --directory
  - ${HOME}/projects/personal/textserve/servers/gordon
  - python
  - -m
  - gordon_mcp
tags:
  - docker
  - cli
health:
  probe: tool-list
  timeout: 10
