protocol: stdio
runtime: claude
native_cmd: npx
native_args:
  - "@sentry/mcp-server@latest"
env:
  - name: SENTRY_ACCESS_TOKEN
    # op path configured in ~/.config/textserve/config.yaml (not committed)
    cache: 1h
tags:
  - monitoring
  - stdio
health:
  probe: tool-list
  timeout: 5
