version: 1

prompts_dir: prompts/
state_dir: logs/
reports_dir: reports/

repos:
  mcp-fleet: ../..

defaults:
  model: sonnet
  budget_usd: 1.50
  max_turns: 100
  claude_cmd: claude-work
  terse: true

max_parallel: 1
total_budget_usd: 20.00
warn_at_pct: 80
